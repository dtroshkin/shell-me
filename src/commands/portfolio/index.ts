import { about } from '@commands/portfolio/about.ts'
import { skills } from '@commands/portfolio/skills.ts'
import { contact } from '@commands/portfolio/contact.ts'

const portfolioCommands = {
  about,
  skills,
  contact
}

export default portfolioCommands
