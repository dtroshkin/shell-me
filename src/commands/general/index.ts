import { clear } from '@commands/general/clear.ts'
import { help } from '@commands/general/help.ts'
import { welcome } from '@commands/general/welcome.ts'

const generalCommands = {
  clear,
  help,
  welcome,
}

export default generalCommands
