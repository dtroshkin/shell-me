import { disco } from '@commands/visual/disco.ts'
import { rainbow } from '@commands/visual/rainbow.ts'
import { poweroff } from '@commands/visual/poweroff.ts'
import { oldscreen } from '@commands/visual/oldscreen.ts'
import { glitch } from '@commands/visual/glitch.ts'

const visualCommands = {
  disco,
  glitch,
  oldscreen,
  poweroff,
  rainbow
}

export default visualCommands
